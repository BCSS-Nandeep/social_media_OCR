# IndicOCR (Bodhan AI / AI4Bharat) poster-OCR pipeline
# Model repo is gated: request access at https://huggingface.co/bodhan-ai/indic-ocr,
# then `hf auth login` (or set HF_TOKEN) before the first run.
# NOTE: torch/transformers are NOT pinned here -- the model's own install.sh
#       (run automatically on first use) detects your CUDA driver and installs
#       matching wheels. Pinning them here would fight that installer.

huggingface_hub>=0.24
opencv-python>=4.10,<5
Pillow>=10.0

# --- HTTP API (src/api.py) ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27          # tests/test_api.py's TestClient, AND runtime: src/media_downloader.py's
                     # safe fetch and src/vlm_client.py's calls to the vLLM server both use it

# --- Video description (src/video/, src/vlm_client.py) ---
# ffmpeg/ffprobe are used via subprocess (src/video/metadata.py), not a Python package --
# install them at the OS level (already present on the deployment host).
# vLLM itself is NOT a dependency of this project: it runs as its own separate process
# serving Qwen2.5-VL-7B-Instruct (see DEPLOYMENT.md's vLLM section) and is talked to only
# over HTTP via httpx above.
