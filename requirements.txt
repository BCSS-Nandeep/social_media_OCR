# PaddleOCR poster-OCR pipeline
# NOTE: PaddlePaddle publishes wheels for CPython 3.8 - 3.12 only.
#       Install these into a Python 3.12 (or 3.10/3.11) virtual environment.

paddlepaddle==3.2.0          # 3.0.0 can't load PP-OCRv5 models ("strides" error);
                             # 3.3.1 hits a PIR executor bug on Windows CPU
                             # ("ConvertPirAttribute2RuntimeAttribute"). 3.2.0 works.
paddleocr>=3.7,<4            # needed for BOTH te_PP-OCRv5_mobile_rec and the
                             # PP-OCRv6 detectors. Together with disabling the
                             # orientation classifier these took document accuracy
                             # from 51.2% to 84.9% on this repo's ground-truth set
                             # (see README, Accuracy).

pandas<3                     # paddlex imports pandas; pandas 3.x ships DLLs that
                             # some corporate Application Control policies block
setuptools                   # paddle.utils.cpp_extension imports it; absent from py3.12 venvs
opencv-python>=4.10,<5
Pillow>=10.0

# --- REST API layer (src/api.py) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9   # required by FastAPI for multipart/form-data uploads
